import java.util.Arrays;
import java.util.Arrays;
import java.util.Scanner;
import java.util.ArrayList;
public class Arraylist1 {
    public static void main(String p[]) {
        //Scanner sc =new Scanner(System.in);
        // Syntax
        //ArrayList<ArrayList<Integer>> list = new ArrayList<>();

        /*list.add(45);
        list.add(45);list.add(455);list.add(475);list.add(454);list.add(453);list.add(4775);
        System.out.println(list);
        list.contains(45); //checking availability of a data in list
        System.out.println(list.contains(45));
        list.set(0,23);
        list.remove(3);
        System.out.println(list);  */


         /*for (int i=0;i<5;i++){
            list.add(sc.nextInt());
        }
        for (int i=0;i<5;i++){
            System.out.println(list.get(i));  //giving index to get the values of list
         */


        //Multi D Arraylist
        //INitialization
        /*for (int i =0;i<3;i++){
            list.add(new ArrayList<>());
          }
        //Adding Elements
        for (int i= 0;i<3;i++){
            for(int j =0;j<3;j++){
                list.get(i).add(sc.nextInt());
            }

        }
        System.out.println(list);

*/

        //SWAP
       /* int [] arr= {1,2,4,5,345};
        swap(arr,0,4);
        System.out.println(Arrays.toString(arr));


    }
    static void swap(int [] arr,int index1,int index2){
        int temp = arr[index1];
        arr[index1]= arr[index2];
        arr[index2]=temp;

    }  */

        //MAX VALUE In ARRAY
        /*int [] a = {23,5,567,4};
        System.out.println(max(a));

    }
    static int max(int arr[]){
        int max = arr[0];
        for(int i=0;i<arr.length;i++) {
            if(arr[i]>max){
                max =arr[i];
            }
        }
        return max;
    }
    */
        //REVERSING THE ARRAY

    }

}
